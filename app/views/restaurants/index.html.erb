<h1>All Restaurants</h1>

<% @restaurants.each do |restaurant| %>
  <%= link_to image_tag(restaurant.image, :class => 'thumb', :alt => restaurant.name), restaurant, :name => restaurant.name %>
<% end %>
